server:
  address: localhost
  port: 9090

spring:
  datasource:
    url: jdbc:h2
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

  h2:
    console:
      path: /h2-console
      enabled: true